import 'package:flutter/cupertino.dart';

class BatchesSlide extends StatelessWidget {
  const BatchesSlide({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Batches are not found"),
      ],
    );
  }
}
